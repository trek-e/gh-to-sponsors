---
phase: 02-content-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/content.ts
  - src/types/config.ts
  - src/types/index.ts
  - src/config/schema.ts
  - package.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Content types exist for commits, digests, and teasers"
    - "Config schema supports array of repos to monitor"
    - "New dependencies installed and importable"
  artifacts:
    - path: "src/types/content.ts"
      provides: "Commit, CommitContext, Digest, Teaser types"
      exports: ["Commit", "CommitContext", "RepoCommitGroup", "Digest", "Teaser", "ActivityPeriod"]
    - path: "src/types/config.ts"
      provides: "Extended GitHubConfig with repos array"
      contains: "repos:"
    - path: "src/config/schema.ts"
      provides: "Zod validation for repos array"
      contains: "repoSchema"
  key_links:
    - from: "src/types/index.ts"
      to: "src/types/content.ts"
      via: "export * from './content.js'"
      pattern: "export.*content"
    - from: "src/config/schema.ts"
      to: "src/types/config.ts"
      via: "type inference"
      pattern: "GitHubConfig"
---

<objective>
Define TypeScript types for content generation and extend configuration schema to support multi-repo monitoring.

Purpose: Establish type contracts that all other Phase 2 plans depend on, ensuring type safety across the content generation pipeline.
Output: Content types (src/types/content.ts), extended config types (src/types/config.ts), and updated Zod schema (src/config/schema.ts) with new dependencies installed.
</objective>

<execution_context>
@/Users/trekkie/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trekkie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-generation/02-CONTEXT.md
@.planning/phases/02-content-generation/02-RESEARCH.md

@src/types/config.ts
@src/types/state.ts
@src/types/index.ts
@src/config/schema.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create content types</name>
  <files>
    - package.json
    - src/types/content.ts
    - src/types/index.ts
  </files>
  <action>
1. Install new dependencies:
   ```bash
   npm install @anthropic-ai/sdk date-fns
   ```

2. Create `src/types/content.ts` with these types (from RESEARCH.md patterns):

```typescript
/**
 * Content generation type definitions
 */

/** Commit type classification based on conventional commits */
export type CommitType = 'feat' | 'fix' | 'docs' | 'chore' | 'refactor' | 'test' | 'ci' | 'perf' | 'style' | 'build' | 'revert' | 'bot' | 'other';

/** Raw commit data from GitHub API (simplified) */
export interface Commit {
  sha: string;
  message: string;
  author: string;
  email: string;
  timestamp: string;
  url: string;
}

/** Classified commit with type */
export interface ClassifiedCommit extends Commit {
  type: CommitType;
  isBot: boolean;
}

/** Prepared commit context for LLM */
export interface CommitContext {
  repo: string;
  displayName: string;
  commits: ClassifiedCommit[];
  botCommitCount: number;
  commitCount: number;
  timeRange: { start: string; end: string };
}

/** Group of commits per repository */
export interface RepoCommitGroup {
  repoName: string;
  displayName: string;
  commits: ClassifiedCommit[];
  botCommitCount: number;
}

/** Activity period result from filtering */
export interface ActivityPeriod {
  commits: ClassifiedCommit[];
  periodType: 'daily' | 'weekly' | 'none';
  hasActivity: boolean;
}

/** Generated digest content */
export interface Digest {
  title: string;
  content: string;
  repos: string[];
  commitCount: number;
  periodType: 'daily' | 'weekly';
  generatedAt: string;
}

/** Generated social teaser */
export interface Teaser {
  text: string;
  hashtags: string[];
  characterCount: number;
}

/** Complete generation result */
export interface GenerationResult {
  digest: Digest;
  teaser: Teaser;
  usage: {
    inputTokens: number;
    outputTokens: number;
  };
}
```

3. Update `src/types/index.ts` to export content types:
   Add `export * from './content.js';` after existing exports.
  </action>
  <verify>
    - `npm ls @anthropic-ai/sdk date-fns` shows both installed
    - `npx tsc --noEmit` passes (types are valid)
  </verify>
  <done>Content types defined with Commit, CommitContext, Digest, Teaser, and related interfaces exported from src/types/index.ts</done>
</task>

<task type="auto">
  <name>Task 2: Extend config for multi-repo support</name>
  <files>
    - src/types/config.ts
    - src/config/schema.ts
  </files>
  <action>
1. Update `src/types/config.ts`:
   - Replace single `owner`/`repo` with `repos` array
   - Add `RepoConfig` interface with owner, repo, displayName (optional)
   - Add `ContentConfig` interface for activity thresholds

```typescript
/** Single repository configuration */
export interface RepoConfig {
  owner: string;
  repo: string;
  displayName?: string;
}

/** Content generation configuration */
export interface ContentConfig {
  /** Minimum commits for daily digest (default: 1) */
  dailyThreshold: number;
  /** Minimum commits for weekly digest (default: 3) */
  weeklyThreshold: number;
}

/** Updated GitHubConfig */
export interface GitHubConfig {
  repos: RepoConfig[];
}
```

Update the main `Config` interface to include `content?: ContentConfig` (optional with defaults).

2. Update `src/config/schema.ts`:
   - Add `repoSchema` for single repo validation (owner, repo required; displayName optional)
   - Add `contentConfigSchema` with defaults (dailyThreshold: 1, weeklyThreshold: 3)
   - Update `githubConfigSchema` to use `repos: z.array(repoSchema).min(1)`
   - Add optional `content` field to main schema with defaults

IMPORTANT: Ensure backward compatibility consideration - the new schema structure changes from single repo to repos array. The validate function should handle this cleanly.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Config schema validates: `{ github: { repos: [{ owner: "test", repo: "test" }] } }` plus required email/approval/schedule fields
  </verify>
  <done>Config supports repos array with optional displayName per repo, content thresholds have sensible defaults</done>
</task>

</tasks>

<verification>
1. `npm ls @anthropic-ai/sdk date-fns` shows both packages installed
2. `npx tsc --noEmit` passes with no errors
3. Types are importable: `import { Commit, Digest, Teaser } from './types/index.js'`
4. Config validation works with new repos array structure
</verification>

<success_criteria>
- @anthropic-ai/sdk and date-fns are installed
- Content types defined in src/types/content.ts
- Config schema extended to support multiple repos
- All TypeScript compilation passes
- Types are exported and importable from src/types/index.ts
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-generation/02-01-SUMMARY.md`
</output>
