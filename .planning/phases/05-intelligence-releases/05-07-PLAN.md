---
phase: 05-intelligence-releases
plan: 07
type: execute
wave: 4
depends_on: ["05-04", "05-05", "05-06"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All tests pass"
    - "TypeScript compiles without errors"
    - "Cadence logic works correctly"
    - "Release workflow is configured"
  artifacts: []
  key_links: []
---

<objective>
Verify Phase 5 implementation is complete and working.

Purpose: Ensure all components integrate correctly before marking phase complete.
Output: Verification of all success criteria met.
</objective>

<execution_context>
@/Users/trekkie/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trekkie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-intelligence-releases/05-CONTEXT.md
@.planning/phases/05-intelligence-releases/05-01-SUMMARY.md
@.planning/phases/05-intelligence-releases/05-02-SUMMARY.md
@.planning/phases/05-intelligence-releases/05-03-SUMMARY.md
@.planning/phases/05-intelligence-releases/05-04-SUMMARY.md
@.planning/phases/05-intelligence-releases/05-05-SUMMARY.md
@.planning/phases/05-intelligence-releases/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite</name>
  <files></files>
  <action>
Run all tests and verify no regressions:

```bash
npm test
```

Expected: All tests pass, including:
- Existing tests (tokens, state, platforms)
- New cadence/detector.test.ts tests
- New releases/content.test.ts tests
  </action>
  <verify>Exit code 0, all tests pass</verify>
  <done>Full test suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify TypeScript compilation</name>
  <files></files>
  <action>
Run TypeScript compiler in check mode:

```bash
npx tsc --noEmit
```

Expected: No errors.
  </action>
  <verify>Exit code 0, no type errors</verify>
  <done>TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Validate workflow files</name>
  <files></files>
  <action>
Validate both workflow files are valid YAML:

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/schedule-digest.yml'))"
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/handle-release.yml'))"
```

Expected: No errors.
  </action>
  <verify>Exit code 0 for both</verify>
  <done>Workflow YAML files are valid.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 5 implementation:
1. Cadence tracking state fields (lastActivityDate, consecutiveQuietDays)
2. Cadence config (mode: daily/weekly/auto, thresholds)
3. Release config (enabled, includePrereleases)
4. decideCadence() function with full test coverage
5. Release content generation with AI
6. handle-release.yml workflow triggered by GitHub release events
7. Updated generate-digest with cadence awareness
8. Release-specific email templates
  </what-built>
  <how-to-verify>
1. Review the implementation summaries in .planning/phases/05-intelligence-releases/
2. Check that all new files exist:
   - src/cadence/detector.ts
   - src/cadence/detector.test.ts
   - src/releases/content.ts
   - src/releases/content.test.ts
   - src/actions/handle-release.ts
   - .github/workflows/handle-release.yml
3. Optionally trigger a manual workflow run to test:
   - `npm run generate-digest` (will skip if no activity, which is correct)
  </how-to-verify>
  <resume-signal>Type "approved" if implementation looks correct, or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- All automated tests pass
- TypeScript compiles without errors
- Workflow files are valid YAML
- Human verification approved
</verification>

<success_criteria>
1. GitHub Releases trigger immediate announcement drafts (handle-release workflow)
2. System falls back to weekly digest when no daily activity (cadence logic)
3. User can configure cadence (daily, weekly, or auto) via config
4. System only sends emails when meaningful activity exists (activity threshold)
5. All tests pass, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-intelligence-releases/05-07-SUMMARY.md`
</output>
