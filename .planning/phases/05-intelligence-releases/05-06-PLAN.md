---
phase: 05-intelligence-releases
plan: 06
type: execute
wave: 4
depends_on: ["05-04", "05-05"]
files_modified:
  - src/email/templates.ts
  - src/actions/send-email.ts
autonomous: true

must_haves:
  truths:
    - "Release emails have distinct styling from digest emails"
    - "Digest emails show period type in subject"
    - "Email templates handle release periodType"
  artifacts:
    - path: "src/email/templates.ts"
      provides: "Updated email templates"
      contains: "release"
    - path: "src/actions/send-email.ts"
      provides: "Updated email sending with release support"
      contains: "periodType.*release"
  key_links:
    - from: "src/actions/send-email.ts"
      to: "src/email/templates.ts"
      via: "uses templates"
      pattern: "import.*from.*templates"
---

<objective>
Update email templates to distinguish release announcements from regular digests.

Purpose: CONTEXT.md says "Releases should feel special/important, not just another digest."
Output: Updated templates with release-specific styling and subject lines.
</objective>

<execution_context>
@/Users/trekkie/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trekkie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-intelligence-releases/05-CONTEXT.md

@src/email/templates.ts
@src/actions/send-email.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update email templates for release announcements</name>
  <files>src/email/templates.ts</files>
  <action>
Update the approval email template to handle release announcements differently:

1. Add release-specific styling:
   - Different header color/icon for releases (e.g., "New Release" badge)
   - Emphasize the release tag prominently
   - "Release Announcement" instead of "Digest"

2. Update subject line generation:
   - For digest: "Daily Update: [repo names]" or "Weekly Update: [repo names]"
   - For release: "New Release: v1.2.0 - [repo name]"

3. Add conditional sections:
   - If periodType === 'release':
     - Show release tag prominently
     - Different intro text emphasizing announcement nature
     - Maybe a "Release" badge/icon

4. Keep backward compatibility:
   - periodType 'daily' | 'weekly' work as before
   - periodType 'release' triggers new styling

Example subject line logic:
```typescript
function getSubjectLine(periodType: string, repos: string[], tag?: string): string {
  if (periodType === 'release' && tag) {
    return `New Release: ${tag} - ${repos[0]}`;
  }
  const period = periodType === 'daily' ? 'Daily' : 'Weekly';
  return `${period} Update: ${repos.join(', ')}`;
}
```
  </action>
  <verify>`npx tsc --noEmit` passes</verify>
  <done>Email templates have distinct release styling and subject lines.</done>
</task>

<task type="auto">
  <name>Task 2: Update send-email to handle release periodType</name>
  <files>src/actions/send-email.ts</files>
  <action>
Update sendApprovalEmail to properly handle the 'release' periodType:

1. Update the EmailContent interface (or equivalent) to accept 'release' as periodType:
```typescript
periodType: 'daily' | 'weekly' | 'release'
```

2. Pass tag name for release subject lines:
   - Extract from summary or add as separate field
   - Or parse from repos array if format is "Release: v1.2.0"

3. Ensure the template function receives periodType to switch styling.

4. Log which type of email is being sent:
```typescript
console.log(`Sending ${periodType} approval email...`);
```
  </action>
  <verify>`npx tsc --noEmit` passes</verify>
  <done>send-email handles release periodType and generates appropriate subject/content.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm test` passes (no regressions)
- Template generates different output for release vs digest
</verification>

<success_criteria>
1. Release emails have "New Release" in subject
2. Release emails visually distinct from digest emails
3. Daily/weekly digests continue to work as before
4. No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-intelligence-releases/05-06-SUMMARY.md`
</output>
