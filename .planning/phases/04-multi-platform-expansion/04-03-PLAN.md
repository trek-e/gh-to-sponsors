---
phase: 04-multi-platform-expansion
plan: 03
type: tdd
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/platforms/mastodon/index.ts
  - src/platforms/mastodon/client.ts
  - src/platforms/mastodon/client.test.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "MastodonPlugin.isConfigured() returns true when instanceUrl and accessToken provided"
    - "MastodonPlugin.isConfigured() returns false when credentials missing"
    - "MastodonPlugin.post() returns success with postId and URL on successful post"
    - "MastodonPlugin.post() returns error without throwing on failure"
    - "MastodonPlugin.post() retries on 429 rate limit with exponential backoff"
    - "MastodonPlugin.post() uses configured visibility setting"
    - "MastodonPlugin.post() includes language tag"
  artifacts:
    - path: "src/platforms/mastodon/client.ts"
      provides: "MastodonPlugin implementing PlatformPlugin"
      exports: ["MastodonPlugin"]
    - path: "src/platforms/mastodon/index.ts"
      provides: "Barrel export"
      contains: "export { MastodonPlugin }"
    - path: "src/platforms/mastodon/client.test.ts"
      provides: "TDD tests for MastodonPlugin"
      contains: "describe('MastodonPlugin'"
  key_links:
    - from: "src/platforms/mastodon/client.ts"
      to: "masto"
      via: "createRestAPIClient import"
      pattern: "import.*createRestAPIClient.*masto"
    - from: "src/platforms/mastodon/client.ts"
      to: "src/platforms/types.ts"
      via: "implements PlatformPlugin"
      pattern: "implements PlatformPlugin"
---

<objective>
Implement MastodonPlugin using TDD following the GhostPlugin pattern, with OAuth token authentication and configurable visibility.

Purpose: Enable teaser posting to Mastodon with proper visibility settings
Output: Tested MastodonPlugin implementation ready for registration
</objective>

<execution_context>
@/Users/trekkie/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trekkie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-multi-platform-expansion/04-CONTEXT.md
@.planning/phases/04-multi-platform-expansion/04-RESEARCH.md
@src/platforms/types.ts
@src/platforms/ghost/client.ts
@src/platforms/ghost/client.test.ts
@src/types/state.ts
</context>

<feature>
  <name>MastodonPlugin</name>
  <files>src/platforms/mastodon/client.ts, src/platforms/mastodon/client.test.ts, src/platforms/mastodon/index.ts</files>
  <behavior>
MastodonPlugin implements PlatformPlugin interface for posting teasers to Mastodon.

Test cases (input -> expected output):

isConfigured():
- (instanceUrl: 'https://mastodon.social', accessToken: 'token123') -> true
- (instanceUrl: undefined, accessToken: 'token123') -> false
- (instanceUrl: 'https://mastodon.social', accessToken: undefined) -> false
- (instanceUrl: '', accessToken: '') -> false

post(state with teaser):
- (state with teaser, mock API success) -> { success: true, platformPostId: '123456', platformUrl: 'https://instance.social/@user/123456' }
- (state without teaser) -> { success: false, error: 'No teaser content' }
- (state, plugin not configured) -> { success: false, error: 'Mastodon not configured' }
- (state, API error) -> { success: false, error: 'error message' } (no throw)
- (state, 429 rate limit) -> retries with exponential backoff

visibility:
- (visibility: 'public') -> posts with visibility: 'public'
- (visibility: 'unlisted') -> posts with visibility: 'unlisted'
- (visibility: 'private') -> posts with visibility: 'private'

name property:
- () -> 'mastodon'
  </behavior>
  <implementation>
Follow GhostPlugin pattern from src/platforms/ghost/client.ts:

1. Create src/platforms/mastodon/client.ts:
   - Import createRestAPIClient from masto
   - Implement PlatformPlugin interface
   - Lazy client creation (on first getClient() call)
   - Use masto.v1.statuses.create() for posting
   - Include visibility parameter from config
   - Include language: 'en' for discoverability
   - Exponential backoff with jitter for 429s (reuse Ghost pattern)
   - Never throw from post() - always return PostResult

2. Create src/platforms/mastodon/index.ts:
   - Barrel export: `export { MastodonPlugin } from './client.js'`

3. Mock masto in tests:
   - Mock createRestAPIClient returning client with v1.statuses.create()
   - Mock status response with id and url fields

4. Post URL: Comes directly from response.url

5. Error handling:
   - Check response.status === 429 for rate limits (or error code 429)
   - Mastodon errors typically have message field
   - 401 for invalid token, 403 for insufficient permissions

Note: Skip content warnings (spoilerText) per CONTEXT.md - not needed for dev updates
Note: Skip instance character limit query - teaser already under 280 chars from Phase 2
  </implementation>
</feature>

<verification>
1. `npm test -- src/platforms/mastodon/client.test.ts` - all tests pass
2. `npx tsc --noEmit` - no type errors
3. Test coverage includes: isConfigured, successful post, error handling, rate limit retry, visibility settings
</verification>

<success_criteria>
- All TDD tests pass
- MastodonPlugin exports from src/platforms/mastodon/index.ts
- Plugin follows never-throw pattern (returns PostResult, doesn't throw)
- Visibility setting configurable (public, unlisted, private)
- Rate limit retry implemented with exponential backoff
- Language tag included in posts
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-platform-expansion/04-03-SUMMARY.md`
</output>
