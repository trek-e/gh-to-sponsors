---
phase: 01-foundation-approval-loop
plan: 07
type: execute
wave: 5
depends_on: ["01-06"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Scheduled workflow runs via manual trigger"
    - "Email is received with approve/skip links"
    - "Clicking approve triggers repository_dispatch"
    - "State shows post as approved"
    - "Clicking same link again shows status page"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of the complete approval loop.

Purpose: Verify that all components work together: scheduling, email sending, token generation, approval endpoint, and state management.

Output: Confirmed working approval loop ready for Phase 2 (content generation) integration.
</objective>

<execution_context>
@/Users/trekkie/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trekkie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-approval-loop/01-CONTEXT.md
@.planning/phases/01-foundation-approval-loop/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy Vercel Function</name>
  <files></files>
  <action>
Deploy the approval endpoint to Vercel:

1. If not already linked:
   - Run: vercel link (follow prompts to connect repo)

2. Set environment variables via CLI:
   - vercel env add APPROVAL_SECRET (paste generated secret)
   - vercel env add GITHUB_TOKEN (paste PAT)
   - vercel env add GITHUB_OWNER (enter username/org)
   - vercel env add GITHUB_REPO (enter repo name)

3. Deploy:
   - Run: vercel --prod
   - Note the deployment URL

4. Test endpoint directly:
   - curl https://your-project.vercel.app/api/approve/invalid-token
   - Should return 403 with error page

5. Update GitHub secrets with deployment URL:
   - APPROVAL_ENDPOINT_URL = https://your-project.vercel.app
  </action>
  <verify>
    vercel --prod succeeds
    curl to invalid token returns 403
  </verify>
  <done>
    Vercel deployment is live
    Approval endpoint responds to requests
    Environment variables are set
  </done>
</task>

<task type="auto">
  <name>Task 2: Run Manual Workflow Test</name>
  <files></files>
  <action>
Trigger the workflow manually and verify email:

1. Ensure all GitHub secrets are set:
   - EMAIL_API_KEY
   - EMAIL_FROM
   - APPROVAL_SECRET
   - APPROVAL_ENDPOINT_URL

2. Trigger workflow via GitHub UI:
   - Go to Actions tab
   - Select "Daily Digest Check" workflow
   - Click "Run workflow"
   - Select branch (main)
   - Click "Run workflow" button

3. Monitor workflow execution:
   - Watch for successful completion
   - Check logs for any errors

4. Verify email received:
   - Check inbox for approval email
   - Verify subject line is correct
   - Verify approve/skip links are present
   - Verify links point to Vercel deployment
  </action>
  <verify>
    Workflow completes successfully
    Email is received within 5 minutes
    Email contains valid approval links
  </verify>
  <done>
    Workflow runs without errors
    Email is delivered
    Links are correctly formatted
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Complete Approval Flow</name>
  <what-built>
Complete approval loop: scheduling, email, approval endpoint, and state management.
  </what-built>
  <how-to-verify>
1. Open the approval email you received

2. Click the "Approve and Post" link
   - You should see a success page: "Success! Your digest will be posted..."
   - Note: Actual platform posting is Phase 3+

3. Go to GitHub Actions and verify:
   - A new workflow run appeared for "Handle Approval"
   - The workflow completed successfully
   - Check the logs to confirm postId and action were received

4. Click the SAME approval link again
   - You should see a status page (not success)
   - Status page should indicate the digest was already approved
   - This confirms replay prevention works

5. Check the state artifact:
   - Go to the latest workflow run
   - Download the "digest-state" artifact
   - Verify it contains the post with status: "approved"
   - Verify usedTokens contains the jti

6. (Optional) Click the "Skip" link from a NEW email:
   - Trigger workflow again to get fresh email
   - Click skip instead of approve
   - Verify skip flow works similarly

Report any issues found.
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- [ ] Vercel deployment is live and responding
- [ ] Workflow runs successfully via manual trigger
- [ ] Email is received with valid links
- [ ] Clicking approve triggers handle-approval workflow
- [ ] Clicking same link shows status (not error)
- [ ] State artifact shows correct post status
- [ ] jti is recorded for replay prevention
</verification>

<success_criteria>
Phase 1 success criteria from ROADMAP.md:
1. GitHub Action runs on schedule without manual intervention - VERIFIED (manual trigger works, schedule will work)
2. Creator receives email with preview of content to be posted - VERIFIED
3. Creator clicks approve link and platforms receive posting trigger - VERIFIED (repository_dispatch triggered)
4. Approval links expire after 24-48 hours and cannot be replayed - VERIFIED (token expiration + jti tracking)
5. System tracks which drafts are pending vs posted - VERIFIED (state artifact)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-approval-loop/01-07-SUMMARY.md`
</output>
