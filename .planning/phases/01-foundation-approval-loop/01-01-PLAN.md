---
phase: 01-foundation-approval-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/types/index.ts
  - src/types/config.ts
  - src/types/state.ts
  - src/config/schema.ts
  - src/config/load.ts
  - gh-to-sponsors.config.example.yaml
autonomous: true

must_haves:
  truths:
    - "Project compiles with TypeScript"
    - "Configuration file validates against schema"
    - "Type definitions exist for all core concepts"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "typescript"
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
      contains: "strict"
    - path: "src/types/index.ts"
      provides: "Core type exports"
      exports: ["Config", "DigestState", "PostState"]
    - path: "src/config/schema.ts"
      provides: "Configuration validation"
      exports: ["validateConfig"]
  key_links:
    - from: "src/config/load.ts"
      to: "src/config/schema.ts"
      via: "imports validateConfig"
      pattern: "validateConfig"
    - from: "src/config/schema.ts"
      to: "src/types/config.ts"
      via: "uses Config type"
      pattern: "Config"
---

<objective>
Set up the project foundation with TypeScript configuration, core type definitions, and user configuration schema.

Purpose: Establish the foundational types and configuration system that all other modules depend on. Without this, nothing else can be built type-safely.

Output: A working TypeScript project with validated configuration loading and all core type definitions for the approval workflow system.
</objective>

<execution_context>
@/Users/trekkie/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trekkie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-approval-loop/01-CONTEXT.md
@.planning/phases/01-foundation-approval-loop/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize TypeScript Project</name>
  <files>
    package.json
    tsconfig.json
  </files>
  <action>
Initialize a Node.js project with TypeScript and essential dependencies:

1. Create package.json with:
   - name: "gh-to-sponsors"
   - type: "module" (ESM)
   - engines: { "node": ">=20.0.0" }
   - scripts: build, test, typecheck
   - dependencies: resend (default email provider), zod (config validation)
   - devDependencies: typescript, @types/node, vitest

2. Create tsconfig.json with:
   - target: "ES2022"
   - module: "NodeNext"
   - moduleResolution: "NodeNext"
   - strict: true
   - outDir: "dist"
   - rootDir: "src"
   - declaration: true

Use Node.js 24.x as the target runtime (as per RESEARCH.md).
Do NOT install @vercel/node yet - that goes in the api/ directory later.
  </action>
  <verify>
    npm install completes without errors
    npx tsc --noEmit passes (once files exist)
  </verify>
  <done>
    package.json exists with correct dependencies
    tsconfig.json exists with strict mode enabled
    npm install succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Core Type Definitions</name>
  <files>
    src/types/index.ts
    src/types/config.ts
    src/types/state.ts
    src/types/email.ts
    src/types/token.ts
  </files>
  <action>
Create TypeScript interfaces for all core concepts:

1. src/types/config.ts - User configuration:
   - EmailProvider: 'resend' | 'ses' | 'sendgrid'
   - EmailConfig: { provider, apiKey, fromEmail, replyTo? }
   - ApprovalConfig: { expirationHours: 24 | 48, autoAction: 'approve' | 'skip' | 'none' }
   - ScheduleConfig: { cronExpression, timezone? }
   - Config: { email, approval, schedule, github: { owner, repo } }

2. src/types/state.ts - Artifact state (from RESEARCH.md):
   - PostState: { id, contentHash, status, platforms, createdAt, approvedAt? }
   - DigestState: { posts: Record<string, PostState>, usedTokens: string[], lastRun: string }
   - PostStatus: 'pending' | 'approved' | 'skipped' | 'posted'
   - PlatformResult: 'success' | 'failed'

3. src/types/email.ts - Email abstractions:
   - EmailParams: { to, subject, html, text? }
   - EmailResult: { success, messageId?, error? }
   - EmailProvider interface: { send(params): Promise<EmailResult> }

4. src/types/token.ts - Token structures:
   - TokenPayload: { postId, action, exp, jti }
   - TokenAction: 'approve' | 'skip'
   - VerificationResult: { valid: true, postId, action, jti } | { valid: false, reason }
   - TokenReason: 'expired' | 'invalid-signature' | 'already-used' | 'malformed'

5. src/types/index.ts - Re-export all types for clean imports

Use branded types where appropriate (e.g., PostId as branded string).
  </action>
  <verify>
    npx tsc --noEmit passes
    All types are exported from src/types/index.ts
  </verify>
  <done>
    All type files exist with complete interfaces
    Types can be imported from 'src/types'
    No TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Configuration Schema and Loader</name>
  <files>
    src/config/schema.ts
    src/config/load.ts
    gh-to-sponsors.config.example.yaml
  </files>
  <action>
Create configuration validation and loading:

1. src/config/schema.ts - Zod schema matching Config type:
   - Validate email provider is one of supported options
   - Validate expirationHours is 24 or 48
   - Validate autoAction is 'approve' | 'skip' | 'none'
   - Validate cronExpression is valid cron format (basic regex)
   - Export validateConfig(unknown): Config (throws on invalid)
   - Export configSchema for type inference

2. src/config/load.ts - Load and validate config:
   - loadConfig(path?: string): Config
   - Default path: 'gh-to-sponsors.config.yaml'
   - Use dynamic import for yaml parsing (use 'yaml' package)
   - Add yaml to dependencies in package.json
   - Throw descriptive error on validation failure

3. gh-to-sponsors.config.example.yaml - Example configuration:
   - Document all options with comments
   - Show Resend as default provider
   - Show 24h expiration as default
   - Show 'none' as autoAction default (require manual approval)
   - Use cron offset from top of hour (e.g., "37 9 * * *")
   - Include github.owner and github.repo placeholders

Honor locked decisions from CONTEXT.md:
- User configurable timeout (24 or 48 hours)
- User configurable auto-action
- User configurable time of day via cron
- Support multiple email providers (abstract interface)
  </action>
  <verify>
    npx tsc --noEmit passes
    npm test (if tests exist) or manual validation with test config
  </verify>
  <done>
    Config schema validates correct configs
    Config schema rejects invalid configs with clear errors
    Example config file documents all options
    loadConfig successfully parses YAML and validates
  </done>
</task>

</tasks>

<verification>
- [ ] npm install completes successfully
- [ ] npx tsc --noEmit passes with no errors
- [ ] All type files exist and export correctly
- [ ] Config validation accepts valid config
- [ ] Config validation rejects invalid config with descriptive error
- [ ] Example config file is complete and documented
</verification>

<success_criteria>
- TypeScript project compiles without errors
- All core types are defined and exported
- Configuration schema validates user config files
- Example configuration demonstrates all options
- Foundation is ready for Wave 2 plans (tokens, state, email)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-approval-loop/01-01-SUMMARY.md`
</output>
